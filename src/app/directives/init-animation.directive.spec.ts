import { InitAnimationDirective } from './init-animation.directive';

describe('InitAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new InitAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
